import { HomeWorldNamePipe } from './home-world-name.pipe';

describe('HomeWorldNamePipe', () => {
  it('create an instance', () => {
    const pipe = new HomeWorldNamePipe();
    expect(pipe).toBeTruthy();
  });
});
