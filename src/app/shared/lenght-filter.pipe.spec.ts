import { LenghtFilterPipe } from './lenght-filter.pipe';

describe('LenghtFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LenghtFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
