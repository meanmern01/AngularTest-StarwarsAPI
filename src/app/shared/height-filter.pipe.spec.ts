import { HeightFilterPipe } from './height-filter.pipe';

describe('HeightFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HeightFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
